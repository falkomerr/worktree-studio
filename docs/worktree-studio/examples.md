# Examples

Build and inspect the project:

```bash
pnpm run build
wts doctor
wts list
wts commands
```

Run documentation commands through Worktree Studio:

```bash
wts run . docs.build
wts pipeline . verify
```

Launch the GUI:

```bash
wts gui --port 0
```
